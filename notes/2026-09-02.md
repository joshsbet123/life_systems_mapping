Chose bereavement and first property purchase as anchor milestones; divorce deferred as the falsification case.
Built taxonomy v0.1 (variants, codebook) then v0.2 (needs, conditions, touchpoints, three edge tables). Bereavement populated for parent and partner anticipated variants only.
Cut valence. Demoted sector to a touchpoint attribute; needs and actor type are the stable layers.
First query run: energy leads at month 0 for a parent's death; probate at month 1. All rows still asserted.
Data-quality lessons: two CSV rows broke on unquoted commas; two edges used class ids instead of variant ids. All caught by parse and foreign-key checks, none visible by eye. Validation script is the next thing to write.
Created GitHub repo. Learning that folders are file paths. Moving files into taxonomy/.
Next: README, folder placeholders, then GitHub Desktop and a local clone before editing anything else.
